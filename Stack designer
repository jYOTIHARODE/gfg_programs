stack<int>_push(int arr[],int n)
{
    stack<int>s;
    int i,b;
    for(i=0;i<n;i++) 
    {
        s.push(arr[i]);
    }
    return s;
}

void _pop(stack<int>s)
{
    while(!s.empty())
    {
        cout<<s.top()<<" "; 
        s.pop();
    }
}
