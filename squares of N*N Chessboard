#include <bits/stdc++.h>
using namespace std;

int main() 
{
    int t; cin>>t;
    while(t--)
    {
        int n; cin>>n;
        if(n==1)
            cout<<"1"<<endl;
        else
        {
            cout<<(n * (n + 1) / 2) * (2*n + 1) / 3<<endl;
        }
    }//code
	return 0;
}
